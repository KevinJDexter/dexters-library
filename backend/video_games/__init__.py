"""
Video games feature package: the VideoGame model and its endpoints.

An __init__.py (even an almost-empty one) is what makes a folder an
importable package, so `from video_games.models import VideoGame` works.
"""
